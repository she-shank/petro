import 'package:flutter/material.dart';

class UploadPost extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Upload Post'),
    );
  }
}
